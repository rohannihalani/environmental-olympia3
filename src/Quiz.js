import "./App.css";

import { Widget } from "@typeform/embed-react";

function Quiz() {
  return (
    <main className="mainQuiz">
      <Widget
        id="jtSo89VY "
        style={{ width: "80vw", height: "80vh", padding: "0", margin: "0" }}
        className="quiz-form"
      />
    </main>
  );
}

export default Quiz;
